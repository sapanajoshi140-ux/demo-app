# Local-development database credentials. Copy to `.env` (git-ignored).
# In AWS these are NOT used — the real values come from GitHub secrets
# (DB_USERNAME / DB_PASSWORD), passed to Ansible. This file only feeds the
# local docker-compose stack.
POSTGRES_USER=appuser
POSTGRES_PASSWORD=appsecret
POSTGRES_DB=appdb
